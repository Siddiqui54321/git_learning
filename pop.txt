#Create new branch
>git branch dev			--create new branch
>git checkout dev		--witch to the new branch
>git switch dev 		--switch to the new branch
>git branch			--check branch
>git checkout -b dev		--Create & switch in ONE command (shortcut)
>git switch -c dev		--Create & switch in ONE command (shortcut)
>git push -u origin dev		--Push branch to GitHub (important)
				--After creating the branch locally, upload it:
>git switch main		--Switch between branches
>git switch dev			--Switch between branches


#Scenario:
You have two branches: main and dev
File pop.txt is currently in main, and you want to:
Send pop.txt to dev
Add a line in dev
Commit in dev
Merge changes back into main
Push to remote
D:\gitwork\git_learning

--Ans
>git branch
>cd /d/gitwork/git_learning
>pwd
>cat pop.txt
>git checkout dev
>nano pop.txt
>git add pop.txt
>git commit -m "updated"	--Changes in dev completed
>git checkout main
>git merge dev			--Changes from dev added to main
>git push origin main
>cd# Planning

>git checkout dev
>git push origin dev
--------------------------------------------30/12/2025

---Git branch       how many branches and current branch

--GIT fetch
 open 'README.md' on hit hub  and edit directly on their add one line
 'Commit changes'   commit changes...commit directly to the main branch
>git chanckout main    on command prompt
>git status
>git fetch          (only pointer update and not apply)
		
>git status
>git merge
>git pull	



1> all the changes remote changes display in local then use >git fetch
>git checkout main
>git fetch  (fetch kabhi bhi local file system main change reflect nahi karta
		woo sirf pointer update karta hay.
>git status
		(agar changes apply karna hay to pull use karni ho gi)
>git merge origin/main 
>git status

>git pull   (pull the combination of fetch and merge when run pull then first 
		it takes fetch then take merge. so origin/main branch ko lay aata hay.



--resolving conflict
dev1 has changes
dev2 also changes a file
I am dev
